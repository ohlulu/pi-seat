{
	"name": "pi-seat",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "seat rewritten as a Pi extension + TS CLI; extension entry declared in the pi.extensions manifest",
	"license": "MIT",
	"pi": {
		"extensions": ["./src/extension/index.ts"]
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"proper-lockfile": "^4.1.2"
	},
	"devDependencies": {
		"@earendil-works/pi-coding-agent": "0.84.2",
		"@types/bun": "^1.2.0",
		"@types/proper-lockfile": "^4.1.4",
		"typescript": "^5.7.0"
	}
}
